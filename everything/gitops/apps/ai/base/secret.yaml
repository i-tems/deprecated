apiVersion: v1
kind: Secret
metadata:
  name: ai
type: Opaque
stringData:
  GOOGLE_CLIENT_ID: REDACTED
  GOOGLE_CLIENT_SECRET: REDACTED
  JWT_SECRET: REDACTED
  ALLOWED_EMAILS: dahuin000@gmail.com,thkdev0519@gmail.com
  BASE_URL: https://ai.i-tems.com
  MAX_CONCURRENT: "3"
  MAX_USD_PER_DAY: "80"
  MAX_PROMPT_CHARS: "50000"
  MAX_UPLOAD_MB: "10"
  REQUEST_TIMEOUT_SEC: "600"
# NOTE: the former `ai-claude-creds` bootstrap Secret was removed. claude
# credentials now come from the shared NFS mount (see claude-share.yaml),
# refreshed in place by the always-on hive worker fleet.
