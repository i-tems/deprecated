apiVersion: v1
kind: Service
metadata:
  name: meetup-scheduler-backend
  labels:
    app: meetup-scheduler-backend
spec:
  type: ClusterIP
  selector:
    app: meetup-scheduler-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
