fastapi==0.115.8
uvicorn[standard]==0.30.6
pydantic==2.10.6
python-dotenv==1.0.1

# For Iceberg-backed DuckDB access (used opportunistically; falls back to defaults if unavailable)
simple-utils[iceberg]==16
pandas>=2.0
