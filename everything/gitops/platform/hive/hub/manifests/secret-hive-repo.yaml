# i-tems/hive private repo clone용 PAT.
# hive-agent-loop·worker pod의 initContainer가 i-tems/hive를 emptyDir에 clone할 때 사용.
# Lab-default 평문 secret. 외부 노출 시 회전.
apiVersion: v1
kind: Secret
metadata:
  name: hive-repo-token
  namespace: hive
type: Opaque
stringData:
  GITHUB_TOKEN: REDACTED
