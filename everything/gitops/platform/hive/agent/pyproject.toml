[project]
name = "hive-agent"
version = "0.1.0"
description = "Hive cell-agent — loop engine + wake API"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "httpx>=0.27",
    "watchfiles>=1.0",
    "kubernetes>=35.0,<36",
    "langfuse>=3.0",
    "claude-agent-sdk>=0.1.81,<0.2",
    "prometheus_client>=0.20",
]
