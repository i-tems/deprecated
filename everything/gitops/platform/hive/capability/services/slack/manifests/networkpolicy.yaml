apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: hive-capability-slack
  namespace: hive
spec:
  podSelector:
    matchLabels:
      app: hive-capability-slack
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: hive-hub
      ports:
        - port: 8000
          protocol: TCP
