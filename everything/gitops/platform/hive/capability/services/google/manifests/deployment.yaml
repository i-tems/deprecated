apiVersion: v1
kind: Service
metadata:
  name: hive-capability-google
  namespace: hive
spec:
  type: ClusterIP
  selector:
    app: hive-capability-google
  ports:
    - name: http
      port: 8000
      targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hive-capability-google
  namespace: hive
spec:
  replicas: 0
  strategy: { type: Recreate }
  selector:
    matchLabels:
      app: hive-capability-google
  template:
    metadata:
      labels:
        app: hive-capability-google
    spec:
      containers:
        - name: google
          image: registry.lab.i-tems.com/hive-capability-google:SEED
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: DATA_DIR
              value: /data
          volumeMounts:
            - name: hive-data
              mountPath: /data
            - name: hive-data
              mountPath: /var/data
              subPath: capabilities/google/data
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests: { cpu: 25m, memory: 64Mi }
            limits:   { cpu: 200m, memory: 256Mi }
      volumes:
        - name: hive-data
          persistentVolumeClaim: { claimName: hive-data }
